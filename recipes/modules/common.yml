modules:
  - type: files
    files:
      - source: system
        destination: / # copies files/system/* (* means everything inside it) into your image's root folder /

  - type: script
    scripts:
      - add-vscode-repo.sh
    snippets:
      - sed -i 's|^SHELL=.*|SHELL=/usr/bin/fish|' /etc/default/useradd
  
  - type: fonts
    fonts:
      nerd-fonts:
        - CascadiaCode
        - Monofur
        - OpenDyslexic
        - DaddyTimeMono

  - type: rpm-ostree
    repos:
      - https://copr.fedorainfracloud.org/coprs/atim/starship/repo/fedora-%OS_VERSION%/atim-starship-fedora-%OS_VERSION%.repo
      - https://dl.winehq.org/wine-builds/fedora/%OS_VERSION%/winehq.repo
    install:
      - micro
      - starship
      - code
      - fish
      - winehq-stable
    optfix:
      - wine-stable

  - type: files
    files:
      - source: skel-config/.config
        destination: /etc/skel/.config
      - source: scripts/update-skel.sh
        destination: /usr/bin/update-dotfiles

  - type: script
    snippets:
      - sed -i 's|^SHELL=.*|SHELL=/usr/bin/fish|' /etc/default/useradd
      - chmod +x /usr/bin/update-dotfiles
      - ujust toggle-user-motd

  - type: brew
    auto-update: true
    auto-upgrade: true
    brew-analytics: false
    nofile-limits: true

  - type: bling
    install:
      - 1password

  - type: files
    files:
      - source: scripts/install-brew-packages.sh
        destination: /etc/profile.d/install-brew-packages.sh
  
  - type: script
    scripts:
      - add-opendeck-udev-rule.sh

  - type: default-flatpaks
    configurations:
      - notify: false # Send notification after install/uninstall is finished (true/false)
        scope: system
        # If no repo information is specified, Flathub will be used by default
        install: # system flatpaks we want all users to have and not remove
          - org.mozilla.firefox
      - notify: false
        scope: user # Also add Flathub user repo, but no user packages
        install:
          - com.discordapp.Discord

