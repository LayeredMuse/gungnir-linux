modules:
  - type: rpm-ostree
    install:
      - fedora-workstation-repositories
    
  - type: script
    scripts:
      - enable-chrome-repo.sh

  - type: rpm-ostree
    install:
      - google-chrome-stable
    optfix:
      - google